/**
 * Seed script placeholder.
 *
 * In a real project, this could:
 * - Validate `data/*.json` schemas
 * - Download/optimize images into `public/images/`
 * - Emit search indexes (e.g. lunr/minisearch) for client-side search
 *
 * This template keeps data local and static by default.
 */

console.log("Seed step: nothing to do (static JSON already present).");
